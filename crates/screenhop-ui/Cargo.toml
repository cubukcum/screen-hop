[package]
name = "screenhop-ui"
version = "0.0.0"
edition.workspace = true
license.workspace = true

[dependencies]
slint = "1"
image = { version = "0.25", default-features = false, features = ["png"] }

[build-dependencies]
slint-build = "1"
